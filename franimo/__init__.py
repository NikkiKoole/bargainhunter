"""franimo.nl adapter.

Shared HTTP, SQLite, export and serve live in `core/`. This package parses
franimo pages and crawls searches whose `"source"` is `"franimo"` (the
default). `python3 -m franimo.scrape` / `export` / `serve` still work.
"""
